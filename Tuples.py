"""Tuplpes, it is a type of data structure which can store different values
It is almost similar to a list which has different key differences.
a tuple cannot be changed or modified"""

coordinates = (4, 8)
print(coordinates)

#a tuple can be found using it's index.

coordinates = (4, 8)
print(coordinates[0])

#you can create a list of tuples.

coordinates = [(4, 8),(3,5),(3,8),(9,2)]
print(coordinates)